import { ISkills} from "../interfaces/ISkills";

export const SkillsTypes = {
    ThunkState: {
        loading: false,
        message: "",
        error: "" ,
        data: [],
    } as ISkills.ThunkState
}