import { globalStyles } from "../../AppGlobalStyles";

const workStyle = {
    workIntroMessage: {
        fontfamily:'',
        fontWeight: "bold",
        fontSize: "30px",
    },
    workIntroHighlight: {
        color: "#5c05f2",
        fontWeight: "bold",
        fontSize: "30px",
    },
    workFilterContainer: {
        ...globalStyles.centeredStyles
    }
}

export default workStyle;