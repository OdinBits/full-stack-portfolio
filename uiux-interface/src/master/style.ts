import { globalStyle } from "../app/globalStyle";

export const style = {
    container: {
        overFlow:'hidden',
        ...globalStyle.maxStrech
    },
    content: {
        ...globalStyle.maxStrech
    }
}
